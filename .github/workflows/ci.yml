name: CI

on:
  push:
  pull_request:

jobs:
  test:
    name: vet + test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: jdx/mise-action@v4
      - name: go vet
        run: go vet ./...
      - name: go test
        run: go test -race ./...

  e2e:
    name: e2e
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: jdx/mise-action@v4
      - name: build
        run: make build
      - uses: azure/setup-kubectl@v5
      - name: e2e
        run: make e2e
